import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.zalik',
  appName: 'zalik',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
